/**
 * Main JavaScript File
 * Entry point for global scripts or initializations not covered by specific modules.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Portfolio Template Loaded Successfully');

    // Example: Add a class to body when loaded to trigger initial animations if needed
    document.body.classList.add('loaded');
});
